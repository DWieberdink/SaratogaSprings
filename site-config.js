/**
 * Public Mapbox token (pk.…) — https://account.mapbox.com/access-tokens/
 * Optional: set dashboardPassword to a non-empty string to enable the login gate.
 */
window.__SaratogaSiteConfig = {
  mapboxAccessToken: "",
  dashboardPassword: "",
};
