/**
 * Mapbox public token (pk.…) — https://account.mapbox.com/access-tokens/
 * Optional: set dashboardPassword to enable the login gate.
 *
 * This file is tracked with empty values so GitHub Pages never 404s on site-config.js.
 * For a working map locally or on Pages, set mapboxAccessToken (do not commit real secrets
 * if your org blocks them; use GitHub repo secrets + Actions to inject on deploy instead).
 */
window.__SaratogaSiteConfig = {
  mapboxAccessToken: "",
  dashboardPassword: "",
};
